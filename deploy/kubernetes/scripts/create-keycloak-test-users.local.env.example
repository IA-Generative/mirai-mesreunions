# ---------------------------------------------------------------------------
# Example local env file for Keycloak test users bootstrap (DO NOT COMMIT real values)
# Copy to: deploy/kubernetes/scripts/create-keycloak-test-users.local.env
# ---------------------------------------------------------------------------

# Base URL of Keycloak
KEYCLOAK_BASE_URL="https://sso.my-domain"

# Realm where test users will be created
KEYCLOAK_REALM="audio-upload"

# Admin realm used to fetch admin token
KEYCLOAK_ADMIN_REALM="master"

# Admin credentials (replace locally)
KEYCLOAK_ADMIN_USER="admin"
KEYCLOAK_ADMIN_PASSWORD="change-me-admin"

# Test users settings
TEST_USER_PASSWORD="change-me-test-user"
USER_PREFIX="testuser"
USER_COUNT="10"
