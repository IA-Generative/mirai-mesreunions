// PR4 alimentera ce shell avec un router + lazy-load des tabs.
// Pour PR0, on émet juste un log pour vérifier que le bundle est bien servi.
console.log('mydevices-web shell.js loaded — Vite bundle OK');
